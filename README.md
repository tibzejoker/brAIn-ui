# brAIn-ui

Human-interface nodes for [brAIn](https://github.com/tibzejoker/brAIn).

| Node | Purpose |
|---|---|
| `chat` | Browser chat UI — type to send to the network, see node responses inline. |

Install via the [brAIn marketplace](https://github.com/tibzejoker/brAIn-store).
